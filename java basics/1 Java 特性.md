## 1 Java 特性

字节码：一套设计用来在Java运行时系统（JVM）下执行的高度优化的指令集。

JVM：Java运行时系统，字节码解释器、(可移植性、安全性的保证）。

JIT：Just In Time，字节码编译器，字节码 -> 可执行代码，但是不是一次性全部编译，只在运行时编译。



## 2 常用语

#### 2.1 健壮

程序失败的两个主要原因：内存管理错误、误操作引起的异常情况（运行时错误）。

内存管理错误：Java有GC机制，管理内存分配和释放。

运行时错误：提供面向对象的异常处理机制。

#### 2.2 解释性和高性能

解释性：Java程序 -> Java字节码 -> 可执行代码，运行时系统JVM保证了解释性。

高性能：优秀设计的字节码经JIT编译技术，转换成高性能的本机代码。

#### 2.3 分布式

Java处理TCP/IP协议。

远程方法调用，RMI，Remote Method Invocation。

#### 2.4 面向对象

计算机程序两类元素组成：代码和数据。

面向过程：代码作用于数据。

面向对象：围绕数据（对象）组织程序。（用数据控制对代码的访问）

封装：将代码及其处理的数据绑定在一起的一种编程机制。（对外提供公有，内部实现私有，黑匣子）

继承：一个对象获得另一个对象属性的过程，支持了分层设计（抽象）的概念。

多态性：允许一个接口被多个同类动作使用的特性，具体动作与应用场合有关。选择用于情形的特定的动作是编译器的任务，记住并使用通用接口即可。

### 3 Code

#### 3.1 Basic

编译器编译：.java -> .class       命令： javac     XXX.java

解释器执行:  run .class			  命令 ：java      XXX

#### 3.2 基础语法 

jump~~~~

#### 3.3 数据类型

8个基本数据类型：字节型byte，短整型short，整型int，长整型long，字符型char，浮点型float，双精度型double，布尔型boolean

整数类型：各数据长度 ——  略

字符类型：Java使用Unicode码代表字符。因此16位长度。

布尔型

变量的作用域和生命周期

自动类型转换和强制类型转换

表达式中类型的自动提升

数组

字符串

#### 3.4 运算符

jump~~~

#### 3.5 控制语句

jump~~~

#### 3.6 类

访问控制：public default protect private 的访问权限

#### 3.7 高级特性

方法重载：多态，Java实现“一个接口，多个方法”泛型的一种方式。

对象作为参数

参数的传递：按值传递，引用调用

嵌套类和内部类

#### 3.8 继承

略

方法重载 动态方法调度

抽象方法

final

Object类

#### 3.9 包和接口

包： （命名空间）略 

接口： 为支持运行时动态方法解决而设计 。

#### 3.10 异常处理

#### 3.11  多线程

线程切换，上下文转换，context switch

同步性，管程monitor

Thread类，Runnable接口

主线程

线程优先级

volatile

线程同步：关键是管程——一个互斥独占锁定的对象/互斥体。

线程间通信：wait(), notify(), notifyAll()。仅在synchronized方法中被调用。

死锁。

挂起、恢复、终止线程。

#### 3.12 I/O

流：字节类，字符类。

在最底层，所有的I/O都是字节形式的，基于字符的流只是为字符处理提供方便有效的方法。

字节流类

字符流类

System类

#### 3.13 lang研究

包装类：略

Process

Runtime：运行时环境类，包括内存管理、执行其他的程序等。

System类：标准I/O，当前时间，数组复制，环境属性等。

Object类：其他所有类的超类。clone、Cloneable接口。

Class类：封装对象或接口运行时的状态，Object定义的getClass获得。

ClassLoader类：抽象类，规定了类如何加载。

Math类

Compiler类

Thread、ThreadGroup、Runnable等

#### 3.13 java.util

##### 3.13.1 类集框架

类集

类集算法

Collections类

##### 3.13.2 更多工具类

处理日期、计算随机数、标记字符串、观测事件等。

#### 3.14 I/O探讨

#### 3.15 序列化

把一个对象状态写入一个字节流的过程。

RMI

#### 3.16 网络

pause —— 2019.7.21















